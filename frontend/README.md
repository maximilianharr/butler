# Frontend

The frontend window provides the main window frame for the butler app. There is a left pane with all the apps and plugin items. The plugins are sorted from top to bottom like this:

- search
- files
- sync
- other plugins
- settings

By default the last icon is selected which was open when closing the app. 
On the right pane, the main window, is where the plugin is located
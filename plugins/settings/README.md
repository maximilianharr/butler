# Settings 

All settings are in json. The settings are located in settings folder. The settings window has a left div section listing all the settings categories, which is one per each settings json file. When the user clicks on one of the categories then the json key value pairs open on the right div section where the user can edit the values.
# Search

Search engine for markdown files. Should be very fast.

Features:
- when the user types in a search termin it automatically starts searching showing the hits in a row
- for each hit the row and the previous/next row is shown
- The search term is highlighted in orange font color in the respective search hit
- When the user clicks on a hit or the previous / next row then the respective file opens at that location